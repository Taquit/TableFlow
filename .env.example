# ─── Base de datos ────────────────────────────────────────────
DB_USER=postgres
DB_PASSWORD=tu_password_segura
DB_NAME=pparty
DATABASE_URL="postgresql://${DB_USER}:${DB_PASSWORD}@db:5432/${DB_NAME}"

# ─── Backend ───────────────────────────────────────────────────
PORT=4000
JWT_SECRET=tu_jwt_secret_largo_y_aleatorio

# URL del frontend para CORS
FRONTEND_URL=http://TU_IP_O_DOMINIO

# ─── Frontend (Vite build arg) ────────────────────────────────
FRONTEND_VITE_API_URL=http://TU_IP_O_DOMINIO/api
